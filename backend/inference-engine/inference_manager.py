# mangages the math and inference process for the recommendation engine
# takes titles from the global-library.csv and turns into vectors
# passes only ones that pass the constraints defined by constraint_discoverer.py to cosine_sim.py